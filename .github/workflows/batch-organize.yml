name: Batch Organize Pending Files (DISABLED)

on:
  workflow_dispatch:

jobs:
  organize:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Disabled
        run: |
          echo "This workflow is disabled."
          echo "File organization is now handled by batch-mover.yml (every 2 hours)."
          echo "Database sync is handled by db-sync.yml (daily at 2 AM)."
          echo "Security scan is handled by the training form content safety check."
